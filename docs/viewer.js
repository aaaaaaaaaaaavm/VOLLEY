import * as THREE from 'https://esm.sh/three@0.180.0';import{OrbitControls}from'https://esm.sh/three@0.180.0/examples/jsm/controls/OrbitControls.js';import{STLLoader}from'https://esm.sh/three@0.180.0/examples/jsm/loaders/STLLoader.js';const el=document.getElementById('cadViewer');if(el){const scene=new THREE.Scene();scene.background=new THREE.Color(0x08111e);const cam=new THREE.PerspectiveCamera(42,1,.1,10000);const renderer=new THREE.WebGLRenderer({antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));el.appendChild(renderer.domElement);scene.add(new THREE.HemisphereLight(0xddeeff,0x18283b,2.2));const key=new THREE.DirectionalLight(0xffffff,3);key.position.set(2,3,4);scene.add(key);const loader=new STLLoader(),group=new THREE.Group();scene.add(group);const root='https://raw.githubusercontent.com/aaaaaaaaaaaavm/VOLLEY/main/cad/stl/';const parts=[['VOLLEY_Track_Gen5.stl',0x8d949d],['VOLLEY_Stator_Gen5.stl',0xb8622f],['VOLLEY_Sled_Gen5.stl',0xbfc7d1],['VOLLEY_Payload_3U_Gen5.stl',0x5b8cc5]];let loaded=0;const controls=new OrbitControls(cam,renderer.domElement);controls.enableDamping=true;controls.autoRotate=true;controls.autoRotateSpeed=.7;parts.forEach(([f,color])=>loader.load(root+f,g=>{g.computeVertexNormals();const m=new THREE.Mesh(g,new THREE.MeshStandardMaterial({color,metalness:.35,roughness:.5}));group.add(m);if(++loaded===parts.length){const box=new THREE.Box3().setFromObject(group),center=box.getCenter(new THREE.Vector3()),size=box.getSize(new THREE.Vector3()).length();group.position.sub(center);cam.position.set(size*.65,size*.45,size*.75);cam.near=size/1000;cam.far=size*10;cam.updateProjectionMatrix();controls.target.set(0,0,0);controls.update()}},undefined,()=>{el.dataset.error='1'}));function resize(){const w=el.clientWidth,h=Math.max(360,Math.min(620,w*.58));renderer.setSize(w,h,false);cam.aspect=w/h;cam.updateProjectionMatrix()}function loop(){requestAnimationFrame(loop);controls.update();renderer.render(scene,cam)}addEventListener('resize',resize);resize();loop()}