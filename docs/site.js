(()=>{const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);$$('[data-gen]').forEach(b=>b.addEventListener('click',()=>{$$('[data-gen]').forEach(x=>x.classList.remove('active'));$$('.generation-panel').forEach(x=>x.classList.remove('active'));b.classList.add('active');$('#gen-'+b.dataset.gen)?.classList.add('active')}));const c=$('#orbitCanvas');if(!c)return;const ctx=c.getContext('2d'),dv=$('#dv'),phase=$('#phase'),hours=$('#hours'),play=$('#playOrbit'),reset=$('#resetOrbit');const MU=398600.4418,R=6378.137,r0=R+500,vc=Math.sqrt(MU/r0),n0=Math.sqrt(MU/r0**3);let raf=0,playing=false,last=0;function solveE(M,e){let E=M;for(let i=0;i<12;i++)E-=(E-e*Math.sin(E)-M)/(1-e*Math.cos(E));return E}function state(delta,t,phi){const v=vc+delta/1000,eps=v*v/2-MU/r0,a=-MU/(2*eps),h=r0*v,e=Math.sqrt(Math.max(0,1+2*eps*h*h/(MU*MU))),isFast=v>=vc;let M0=isFast?0:Math.PI;const n=Math.sqrt(MU/a**3),M=M0+n*t,E=solveE(M,e),x0=a*(Math.cos(E)-e),y0=a*Math.sqrt(1-e*e)*Math.sin(E);const x=isFast?x0:-x0,y=isFast?y0:-y0,co=Math.cos(phi),si=Math.sin(phi);return{x:x*co-y*si,y:x*si+y*co,a,e}}function draw(){const d=+dv.value,p=+phase.value*Math.PI/180,t=+hours.value*3600;$('#dvVal').textContent=(d>=0?'+':'')+d.toFixed(1)+' m/s';$('#phaseVal').textContent=phase.value+'°';$('#hoursVal').textContent=(+hours.value).toFixed(2).replace(/\.00$/,'')+' h';const s=state(d,t,p),ra=s.a*(1+s.e),rp=s.a*(1-s.e);$('#apo').textContent=(ra-R).toFixed(1)+' km';$('#peri').textContent=(rp-R).toFixed(1)+' km';$('#ecc').textContent=s.e.toFixed(5);const hostAng=p+n0*t,hx=r0*Math.cos(hostAng),hy=r0*Math.sin(hostAng),sep=Math.hypot(s.x-hx,s.y-hy);$('#sep').textContent=sep.toFixed(1)+' km';const rect=c.getBoundingClientRect(),DPR=Math.min(devicePixelRatio||1,2);c.width=rect.width*DPR;c.height=rect.height*DPR;ctx.setTransform(DPR,0,0,DPR,0,0);const w=rect.width,hgt=rect.height,cx=w/2,cy=hgt/2,scale=Math.min(w,hgt)/(2*(R+900));ctx.clearRect(0,0,w,hgt);ctx.strokeStyle='#21364e';ctx.lineWidth=1;for(const alt of[200,500,800]){ctx.beginPath();ctx.arc(cx,cy,(R+alt)*scale,0,Math.PI*2);ctx.stroke()}const g=ctx.createRadialGradient(cx-40,cy-40,20,cx,cy,R*scale);g.addColorStop(0,'#2b78c5');g.addColorStop(.72,'#12385f');g.addColorStop(1,'#081525');ctx.fillStyle=g;ctx.beginPath();ctx.arc(cx,cy,R*scale,0,Math.PI*2);ctx.fill();ctx.strokeStyle='#60738a';ctx.setLineDash([6,7]);ctx.beginPath();ctx.arc(cx,cy,r0*scale,0,Math.PI*2);ctx.stroke();ctx.setLineDash([]);ctx.strokeStyle='#3fd1c5';ctx.lineWidth=2;ctx.beginPath();for(let i=0;i<=240;i++){const tt=i/240*2*Math.PI/Math.sqrt(MU/s.a**3),q=state(d,tt,p),X=cx+q.x*scale,Y=cy-q.y*scale;i?ctx.lineTo(X,Y):ctx.moveTo(X,Y)}ctx.stroke();function dot(x,y,color,r=5){ctx.fillStyle=color;ctx.beginPath();ctx.arc(cx+x*scale,cy-y*scale,r,0,Math.PI*2);ctx.fill()}dot(hx,hy,'#eaf2ff',6);dot(s.x,s.y,'#3fd1c5',6);ctx.font='12px ui-monospace,monospace';ctx.fillStyle='#9fb1c6';ctx.fillText('host',cx+hx*scale+10,cy-hy*scale-8);ctx.fillStyle='#3fd1c5';ctx.fillText('payload',cx+s.x*scale+10,cy-s.y*scale+16);ctx.fillStyle='#91a5bd';ctx.fillText('illustrative two-body browser view · not the validated mission solver',18,hgt-18)}function stop(){playing=false;cancelAnimationFrame(raf);raf=0;last=0;if(play)play.textContent='▶ Animate 24 h'}function tick(now){if(!playing)return;if(!last)last=now;const dt=Math.min(50,now-last);last=now;let h=+hours.value+dt*(24/12000);if(h>=24)h=0;hours.value=h;draw();raf=requestAnimationFrame(tick)}function toggle(){if(playing){stop();return}if(matchMedia('(prefers-reduced-motion: reduce)').matches)return;playing=true;if(play)play.textContent='Ⅱ Pause';raf=requestAnimationFrame(tick)}[dv,phase,hours].forEach(x=>x?.addEventListener('input',()=>{if(x===hours&&playing)stop();draw()}));play?.addEventListener('click',toggle);reset?.addEventListener('click',()=>{stop();dv.value=16;phase.value=20;hours.value=6;draw()});addEventListener('resize',draw);draw()})();